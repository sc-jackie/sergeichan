# Technical solutions

Post-fix learnings and patterns from this repo. Linked from `~/Jackie-OS/Vault/Agent-Learnings/Personal Website learnings.md`.

When writing a fix, capture the learning via `jos-compound` (in-session) — it auto-scaffolds the YAML frontmatter and index row.
